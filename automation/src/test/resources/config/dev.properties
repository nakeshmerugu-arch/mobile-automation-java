# Dev — Android (LaunchAppTest, SampleFeatureTest). For macOS desktop use -Denv=mac (config/mac.properties).
# Prerequisites: Appium UiAutomator2, device/emulator (adb devices). Place your APK at the path below (or change it).

appium.server.url=http://127.0.0.1:4723
platformName=Android
appium\:automationName=UiAutomator2
# Relative to automation/ when using mvn -pl automation (repo root is one level up).
app=../apps/ajaib-android.apk
