# Environment-agnostic defaults. Override per env (dev/stg/prod).
# No credentials or secrets here. Use env vars or CI secrets for sensitive data.
environment=dev

# Driver config (optional). When both are set, BaseTest initializes the driver.
# appium.server.url=http://127.0.0.1:4723
# platformName=Android
